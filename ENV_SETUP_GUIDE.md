# دليل إعداد ملف Environment Variables 🔐

## ملف `.env.local` تم إنشاؤه! ✅

الآن تحتاج إلى ملء القيم الحقيقية بدلاً من `your_xxx_here`

---

## 1️⃣ الحصول على Agora Credentials

### الخطوات:

1. اذهب إلى: https://console.agora.io
2. سجل دخول (أو أنشئ حساب مجاني)
3. اذهب إلى: `Projects`
4. اضغط `Create` لإنشاء مشروع جديد (أو اختر مشروع موجود)
5. انسخ `App ID`

### في ملف `.env.local`:

```bash
NEXT_PUBLIC_AGORA_APP_ID=1234567890abcdef1234567890abcdef
```

**ملاحظة:** Token اختياري للتطوير. يمكنك تركه فارغاً.

---

## 2️⃣ الحصول على Pusher Credentials

### الخطوات:

1. اذهب إلى: https://dashboard.pusher.com
2. سجل دخول (أو أنشئ حساب مجاني)
3. اضغط `Create app` (أو اختر تطبيق موجود)
4. في إعدادات التطبيق، اذهب إلى: `App Keys`
5. انسخ القيم التالية:
   - `app_id` → `PUSHER_APP_ID`
   - `key` → `NEXT_PUBLIC_PUSHER_KEY`
   - `secret` → `PUSHER_SECRET`
   - `cluster` → `NEXT_PUBLIC_PUSHER_CLUSTER`

### في ملف `.env.local`:

```bash
NEXT_PUBLIC_PUSHER_KEY=abc123def456
NEXT_PUBLIC_PUSHER_CLUSTER=eu
PUSHER_APP_ID=1234567
PUSHER_SECRET=1234567890abcdef1234
```

---

## 3️⃣ تفعيل Client Events في Pusher (مهم جداً!)

⚠️ **بدون هذا، الدردشة الكتابية لن تعمل!**

1. في Pusher Dashboard
2. اختر التطبيق
3. اذهب إلى: `App Settings`
4. ابحث عن: `Client events`
5. ✅ فعّل `Enable client events`
6. اضغط `Update` أو `Save`

---

## 4️⃣ التحقق من الملف

بعد ملء القيم، يجب أن يبدو ملف `.env.local` هكذا:

```bash
# Agora
NEXT_PUBLIC_AGORA_APP_ID=1234567890abcdef1234567890abcdef

# Pusher - Public
NEXT_PUBLIC_PUSHER_KEY=abc123def456
NEXT_PUBLIC_PUSHER_CLUSTER=eu

# Pusher - Private
PUSHER_APP_ID=1234567
PUSHER_SECRET=1234567890abcdef1234
```

---

## 5️⃣ إعادة تشغيل السيرفر

بعد تعديل `.env.local`، يجب إعادة تشغيل السيرفر:

```bash
# أوقف السيرفر الحالي (Ctrl+C)
# ثم شغله من جديد:
npm run dev
```

---

## 6️⃣ للنشر على Netlify

**لا ترفع `.env.local` إلى GitHub!** (محمي تلقائياً بـ .gitignore)

بدلاً من ذلك، أضف المتغيرات في Netlify Dashboard:

1. https://app.netlify.com
2. اختر موقعك
3. `Site settings` → `Environment variables`
4. أضف نفس المتغيرات (بدون علامات #)

**المتغيرات المطلوبة في Netlify:**

```
PUSHER_APP_ID=1234567
PUSHER_SECRET=1234567890abcdef1234
NEXT_PUBLIC_PUSHER_KEY=abc123def456
NEXT_PUBLIC_PUSHER_CLUSTER=eu
NEXT_PUBLIC_AGORA_APP_ID=1234567890abcdef1234567890abcdef
```

---

## 🔒 أمان المتغيرات

### ✅ آمن (Public):
- `NEXT_PUBLIC_AGORA_APP_ID`
- `NEXT_PUBLIC_PUSHER_KEY`
- `NEXT_PUBLIC_PUSHER_CLUSTER`

هذه المتغيرات تُرسل للمتصفح ويمكن رؤيتها في الكود المصدري.

### 🔐 خاص (Private):
- `PUSHER_APP_ID`
- `PUSHER_SECRET`

هذه المتغيرات للـ server-side فقط (Netlify Functions).
⚠️ **لا تشارك PUSHER_SECRET مع أحد!**

---

## ✅ التحقق من التكوين

بعد إعداد كل شيء، تحقق من:

1. ✅ ملف `.env.local` موجود ومملوء
2. ✅ Client Events مفعلة في Pusher
3. ✅ السيرفر يعمل بدون أخطاء
4. ✅ الدردشة الصوتية تعمل
5. ✅ الدردشة الكتابية تعمل

---

## 🆘 حل المشاكل

### مشكلة: "AGORA_APP_ID is required"
**الحل:** تأكد من كتابة `NEXT_PUBLIC_AGORA_APP_ID` بشكل صحيح في `.env.local`

### مشكلة: "401 Unauthorized" من Pusher
**الحل:** تحقق من `PUSHER_APP_ID` و `PUSHER_SECRET`

### مشكلة: الرسائل لا تُرسل
**الحل:** تأكد من تفعيل Client Events في Pusher Dashboard

### مشكلة: المتغيرات لا تعمل
**الحل:** أعد تشغيل `npm run dev`

---

## 📝 الخطوات التالية

بعد إعداد `.env.local`:

1. شغل السيرفر: `npm run dev`
2. اختبر التطبيق محلياً
3. ارفع الكود إلى GitHub: `git push origin main`
4. أضف المتغيرات في Netlify Dashboard
5. انتظر النشر
6. اختبر على الموبايل!

---

**جاهز للبدء؟** افتح ملف `.env.local` واملأ القيم! 🚀
