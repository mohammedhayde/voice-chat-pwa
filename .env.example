# Agora Credentials
NEXT_PUBLIC_AGORA_APP_ID=your_agora_app_id_here
# NEXT_PUBLIC_AGORA_TOKEN=optional_temp_token_for_testing

# Pusher Credentials (Public - Client Side)
NEXT_PUBLIC_PUSHER_KEY=your_pusher_key_here
NEXT_PUBLIC_PUSHER_CLUSTER=eu

# Pusher Credentials (Private - Server Side - Netlify Environment Variables)
PUSHER_APP_ID=your_pusher_app_id_here
PUSHER_SECRET=your_pusher_secret_here

# Important Notes:
# 1. Copy this file to .env.local for local development
# 2. For Netlify deployment, add PUSHER_APP_ID and PUSHER_SECRET
#    to Netlify Environment Variables in the dashboard
# 3. Never commit .env.local or any file containing real credentials to git!
