# 🔐 ميزة تغيير كلمة المرور

تم إضافة صفحة كاملة لتغيير كلمة المرور للمستخدمين المسجلين.

## 📍 الموقع

**الرابط:** `/change-password`

**الوصول:** http://localhost:3000/change-password

## ✨ المميزات

### 1. **حماية الصفحة**
- ✅ فقط المستخدمين المسجلين (ليس الضيوف) يمكنهم الوصول
- ✅ إعادة توجيه تلقائية للـ login إذا لم يكن المستخدم مسجل
- ✅ إعادة توجيه للصفحة الرئيسية إذا كان المستخدم ضيف

### 2. **التحقق من البيانات**
- ✅ التحقق من قوة كلمة المرور الجديدة
- ✅ التحقق من تطابق كلمة المرور مع التأكيد
- ✅ منع استخدام نفس كلمة المرور القديمة
- ✅ عرض أخطاء التحقق مباشرة أثناء الكتابة

### 3. **تجربة مستخدم ممتازة**
- ✅ واجهة جميلة ومتجاوبة
- ✅ رسائل نجاح وخطأ واضحة
- ✅ مؤشر تحميل أثناء المعالجة
- ✅ تعطيل الحقول بعد النجاح
- ✅ إعادة توجيه تلقائية بعد النجاح

### 4. **الأمان**
- ✅ استخدام API endpoint المحمي
- ✅ إرسال Access Token مع الطلب
- ✅ تسجيل خروج من جميع الأجهزة بعد التغيير (حسب الـ backend)

## 🎯 كيفية الاستخدام

### للمستخدم:

1. **من الصفحة الرئيسية:**
   - ابحث عن زر "تغيير كلمة المرور 🔐" في الأعلى
   - اضغط عليه للانتقال لصفحة تغيير كلمة المرور

2. **ملء النموذج:**
   - أدخل كلمة المرور الحالية
   - أدخل كلمة المرور الجديدة (يجب أن تكون قوية)
   - أكد كلمة المرور الجديدة
   - اضغط "تغيير كلمة المرور"

3. **بعد النجاح:**
   - سترى رسالة نجاح
   - سيتم تحويلك للصفحة الرئيسية بعد ثانيتين

### للضيف:

- زر "تغيير كلمة المرور" **مخفي** تلقائياً
- إذا حاول الوصول مباشرةً عبر الرابط، سيتم تحويله للصفحة الرئيسية

## 📋 متطلبات كلمة المرور

كلمة المرور الجديدة يجب أن:
- ✅ تحتوي على **8 أحرف** على الأقل
- ✅ تحتوي على **حرف كبير** واحد على الأقل (A-Z)
- ✅ تحتوي على **حرف صغير** واحد على الأقل (a-z)
- ✅ تحتوي على **رقم** واحد على الأقل (0-9)
- ✅ تكون **مختلفة** عن كلمة المرور الحالية

## 📡 API Integration

### Endpoint المستخدم:

```
POST /api/auth/change-password
```

### Request:

```json
{
  "currentPassword": "OldPassword123!",
  "newPassword": "NewSecurePassword456!"
}
```

### Headers:

```
Content-Type: application/json
Authorization: Bearer {accessToken}
```

### Response (Success):

```json
{
  "message": "Password changed successfully"
}
```

### Response (Error):

```json
{
  "message": "Current password is incorrect"
}
```

## 🔧 الملفات المضافة/المعدلة

### 1. app/change-password/page.tsx (جديد)
صفحة كاملة لتغيير كلمة المرور مع:
- نموذج تغيير كلمة المرور
- التحقق من البيانات
- معالجة الأخطاء
- رسائل النجاح والخطأ

### 2. app/page.tsx (معدل)
- إضافة زر "تغيير كلمة المرور" في شريط العنوان
- الزر مخفي للضيوف
- الزر يظهر فقط للمستخدمين المسجلين

### 3. lib/authService.ts (موجود مسبقاً)
- دالة `changePassword()` موجودة وجاهزة
- تتصل بالـ API المحدد في التوثيق

## 💻 الكود

### دالة تغيير كلمة المرور (lib/authService.ts):

```typescript
export async function changePassword(
  currentPassword: string,
  newPassword: string
): Promise<void> {
  const accessToken = localStorage.getItem('accessToken');

  if (!accessToken) {
    throw new Error('غير مصرح');
  }

  const response = await fetch(`${API_BASE_URL}/change-password`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${accessToken}`,
    },
    body: JSON.stringify({ currentPassword, newPassword }),
  });

  const data = await response.json();

  if (!response.ok) {
    throw new Error(data.message || 'فشل في تغيير كلمة المرور');
  }
}
```

### استخدام الدالة في الصفحة:

```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();

  // التحقق من البيانات...

  setLoading(true);

  try {
    await changePassword(formData.currentPassword, formData.newPassword);
    setSuccess(true);

    // إعادة التوجيه بعد ثانيتين
    setTimeout(() => {
      router.push('/');
    }, 2000);
  } catch (err: any) {
    setError(err.message || 'حدث خطأ في تغيير كلمة المرور');
  } finally {
    setLoading(false);
  }
};
```

## 🎨 الواجهة

### الألوان والتصميم:
- 🔐 أيقونة قفل كبيرة في الأعلى
- 🎨 تصميم متناسق مع بقية التطبيق
- 🌈 خلفية متحركة (animated blobs)
- ✨ تأثيرات hover وتحولات سلسة

### الحالات:
1. **الحالة العادية:** نموذج قابل للتعبئة
2. **أثناء التحميل:** زر معطل مع مؤشر دوران
3. **عند النجاح:** رسالة خضراء + زر معطل
4. **عند الخطأ:** رسالة حمراء + إمكانية المحاولة مرة أخرى

## 🧪 الاختبار

### سيناريوهات الاختبار:

1. **تغيير ناجح:**
   - أدخل كلمة مرور حالية صحيحة
   - أدخل كلمة مرور جديدة قوية
   - أكد كلمة المرور
   - النتيجة: نجاح + إعادة توجيه

2. **كلمة مرور حالية خاطئة:**
   - أدخل كلمة مرور حالية خاطئة
   - النتيجة: رسالة خطأ "Current password is incorrect"

3. **كلمة مرور ضعيفة:**
   - أدخل كلمة مرور أقل من 8 أحرف
   - النتيجة: رسالة "يجب أن تكون 8 أحرف على الأقل"

4. **كلمة مرور غير متطابقة:**
   - أدخل كلمة مرور مختلفة في التأكيد
   - النتيجة: رسالة "كلمتا المرور غير متطابقتين"

5. **نفس كلمة المرور:**
   - أدخل نفس كلمة المرور الحالية كجديدة
   - النتيجة: رسالة "كلمة المرور الجديدة يجب أن تكون مختلفة"

6. **وصول كضيف:**
   - سجل دخول كضيف
   - حاول الوصول لـ `/change-password`
   - النتيجة: إعادة توجيه للصفحة الرئيسية

7. **وصول بدون تسجيل:**
   - لا تسجل دخول
   - حاول الوصول لـ `/change-password`
   - النتيجة: إعادة توجيه لـ `/login`

## 🔒 الأمان

### ما تم تنفيذه:
- ✅ التحقق من Access Token
- ✅ إرسال Token في Authorization Header
- ✅ عدم إظهار كلمة المرور في الكود
- ✅ استخدام type="password" للحقول
- ✅ التحقق من صحة كلمة المرور في Frontend والـ Backend

### ما يجب على الـ Backend تنفيذه:
- ⚠️ التحقق من كلمة المرور الحالية
- ⚠️ تشفير كلمة المرور الجديدة
- ⚠️ حذف جميع Refresh Tokens القديمة
- ⚠️ إجبار إعادة تسجيل الدخول على الأجهزة الأخرى

## 📝 ملاحظات

### زر "تغيير كلمة المرور" في الصفحة الرئيسية:

```typescript
{!user?.isGuest && (
  <Link
    href="/change-password"
    className="bg-blue-500/80 hover:bg-blue-600 text-white font-bold px-6 py-3 rounded-2xl backdrop-blur-xl border border-white/20 transition-all hover:scale-105"
  >
    تغيير كلمة المرور 🔐
  </Link>
)}
```

- يظهر فقط إذا لم يكن المستخدم ضيف
- تصميم متناسق مع زر تسجيل الخروج
- تأثيرات hover جميلة

## 🎉 الخلاصة

تم إضافة ميزة تغيير كلمة المرور بنجاح مع:
- ✅ صفحة كاملة ومحمية
- ✅ زر في الصفحة الرئيسية
- ✅ التحقق الكامل من البيانات
- ✅ تجربة مستخدم ممتازة
- ✅ تكامل كامل مع API

الميزة جاهزة للاستخدام! 🚀
