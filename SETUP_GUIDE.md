# 🎙️ دليل إعداد تطبيق الدردشة الصوتية والنصية

هذا التطبيق يجمع بين **Agora** للمحادثة الصوتية و **Pusher** للدردشة النصية.

---

## 📋 المتطلبات

- Node.js 18 أو أحدث
- حساب [Agora](https://console.agora.io) مجاني (للصوت)
- حساب [Pusher](https://dashboard.pusher.com) مجاني (للنص)

---

## 🔧 الإعداد السريع

### 1️⃣ إعداد Agora (للمحادثة الصوتية)

#### الخطوة 1: إنشاء مشروع Agora
1. سجل في [Agora Console](https://console.agora.io)
2. اضغط على "Create a new project"
3. اختر **App ID only** (لا تحتاج Token للاختبار)
4. انسخ **App ID**

#### الخطوة 2: تحديث المشروع
افتح `app/page.tsx` وحدث:
```typescript
const AGORA_APP_ID = 'YOUR_AGORA_APP_ID'; // الصق App ID هنا
const AGORA_TOKEN = ''; // اتركه فارغاً للاختبار
```

#### ملاحظة مهمة:
- إذا قمت بتفعيل **Primary Certificate** في Agora Console، ستحتاج إلى Token
- للحصول على Token مؤقت: [Agora Token Builder](https://console.agora.io)
- يُنصح بتعطيل Primary Certificate للاختبار

---

### 2️⃣ إعداد Pusher (للدردشة النصية)

#### الخطوة 1: إنشاء تطبيق Pusher
1. سجل في [Pusher Dashboard](https://dashboard.pusher.com)
2. اضغط على "Create new app"
3. اختر:
   - **Name**: أي اسم (مثل: voice-chat-app)
   - **Cluster**: اختر الأقرب لموقعك (eu، us2، ap1، وغيرها)
   - **Tech stack**: اختر React (اختياري)
4. اضغط "Create app"

#### الخطوة 2: الحصول على المفاتيح
1. اذهب إلى **App Keys** في لوحة التحكم
2. انسخ:
   - **app_id**
   - **key** (هذا هو PUSHER_APP_KEY)
   - **cluster**

#### الخطوة 3: تفعيل Client Events (مهم جداً!)
1. في لوحة التحكم Pusher، اذهب إلى **App Settings**
2. فعّل **"Enable client events"**
3. احفظ التغييرات

**⚠️ بدون تفعيل Client Events، لن تعمل الدردشة النصية!**

#### الخطوة 4: تحديث المشروع
افتح `app/page.tsx` وحدث:
```typescript
const PUSHER_APP_KEY = 'YOUR_PUSHER_KEY'; // الصق key هنا
const PUSHER_CLUSTER = 'eu'; // أو us2، ap1، حسب اختيارك
```

---

## 🚀 تشغيل التطبيق

### 1. تثبيت المكتبات
```bash
npm install --legacy-peer-deps
```

### 2. التشغيل في وضع التطوير
```bash
npm run dev
```

### 3. فتح التطبيق
افتح [http://localhost:3000](http://localhost:3000) في المتصفح

---

## 📱 الاستخدام

1. **اختبر الميكروفون**: اضغط على زر اختبار الميكروفون في الصفحة الرئيسية
2. **اختر غرفة**: اختر إحدى الغرف العشر المتاحة
3. **أدخل اسمك**: اكتب اسمك للمشاركة
4. **انضم للغرفة**:
   - اضغط "انضم إلى الغرفة" للمحادثة الصوتية عبر Agora
   - ستفتح الدردشة النصية تلقائياً عبر Pusher
5. **شارك الرابط**: أرسل اسم الغرفة لأصدقائك للانضمام

---

## 🔧 هيكل المشروع

```
voice-chat-pwa/
├── app/
│   └── page.tsx              # الصفحة الرئيسية (اختيار الغرف)
├── components/
│   └── VoiceChatRoom.tsx     # واجهة الغرفة (صوت + نص)
├── hooks/
│   ├── useAgoraVoice.ts      # منطق المحادثة الصوتية (Agora)
│   └── usePusherChat.ts      # منطق الدردشة النصية (Pusher)
└── package.json
```

---

## 🐛 حل المشاكل الشائعة

### مشكلة: "الميكروفون غير متاح"
- **السبب**: إذن الوصول للميكروفون مرفوض
- **الحل**:
  - تأكد من منح إذن الوصول للميكروفون في المتصفح
  - جرب المتصفح من https أو localhost
  - تحقق من إعدادات الخصوصية في نظام التشغيل

### مشكلة: "CAN_NOT_GET_GATEWAY_SERVER" من Agora
- **السبب**: Primary Certificate مفعّل لكن لم يتم توفير Token
- **الحل**:
  - خيار 1: عطّل Primary Certificate في Agora Console
  - خيار 2: احصل على Token من [Token Builder](https://console.agora.io)

### مشكلة: الدردشة النصية لا ترسل رسائل
- **السبب**: Client Events غير مفعّلة في Pusher
- **الحل**:
  - اذهب إلى App Settings في Pusher Dashboard
  - فعّل "Enable client events"
  - احفظ التغييرات وأعد تحميل الصفحة

### مشكلة: "⚠️ تنبيه: يلزم تكوين Pusher"
- **السبب**: لم يتم تحديث مفاتيح Pusher
- **الحل**: تأكد من تحديث `PUSHER_APP_KEY` و `PUSHER_CLUSTER` في `app/page.tsx`

---

## 🌐 النشر على Netlify

التطبيق جاهز للنشر! اتبع الخطوات التالية:

### 1. رفع المشروع على GitHub
```bash
git add .
git commit -m "Configure Pusher and Agora"
git push origin main
```

### 2. النشر على Netlify
1. سجل في [Netlify](https://netlify.com)
2. اضغط "New site from Git"
3. اختر مستودع GitHub الخاص بك
4. الإعدادات ستكون تلقائية:
   - Build command: `npm run build`
   - Publish directory: `out`
5. اضغط "Deploy site"

### 3. تحديث المفاتيح للإنتاج
- بعد النشر، قد تحتاج لتحديث CORS settings في Pusher Dashboard
- أضف نطاق Netlify الخاص بك إلى Allowed Origins

---

## 📊 حدود الاستخدام المجاني

### Agora (الصوت)
- **10,000 دقيقة شهرياً** مجاناً
- مناسب للاختبار والاستخدام الشخصي

### Pusher (النص)
- **200,000 رسالة يومياً** مجاناً
- **100 اتصال متزامن**
- مناسب للتطبيقات الصغيرة والمتوسطة

---

## 🔒 الأمان

**⚠️ تحذير**: المفاتيح في `app/page.tsx` ستكون مرئية في الكود الأمامي!

### للإنتاج، يُنصح بـ:
1. **Agora Token Server**: إنشاء خادم لتوليد Tokens ديناميكياً
2. **Pusher Authorization**: استخدام خادم خلفي للتحقق من الصلاحيات
3. **متغيرات البيئة**: استخدام `.env.local` لتخزين المفاتيح السرية

---

## 💡 ميزات إضافية يمكن إضافتها

- [ ] غرف خاصة بكلمات مرور
- [ ] تسجيل المحادثات الصوتية
- [ ] إرسال الصور في الدردشة النصية
- [ ] قائمة المستخدمين المتصلين في الوقت الفعلي
- [ ] إشعارات الرسائل الجديدة
- [ ] وضع المشرف لإدارة الغرف

---

## 📞 الدعم الفني

إذا واجهت أي مشاكل:
1. تحقق من console المتصفح للأخطاء
2. راجع وثائق [Agora](https://docs.agora.io)
3. راجع وثائق [Pusher](https://pusher.com/docs)

---

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

---

**استمتع بالدردشة! 🎉**
